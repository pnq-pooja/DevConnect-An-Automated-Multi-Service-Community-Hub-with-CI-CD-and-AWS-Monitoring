---
apiVersion: v1
kind: Service
metadata:
  name: DevConnect-app
  labels:
    app: DevConnect-app
spec:
  ports:
  - name: http
    port: 3000
    protocol: TCP
    targetPort: 3000
  selector:
    app: DevConnect-app
  type: LoadBalancer
...
